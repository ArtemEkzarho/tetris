import { createTheme } from '@mui/material'

export const theme = createTheme({
  typography: {
    fontFamily: '"Space Mono", monospace',
    fontSize: 14,
    h6: {
      fontSize: 12,
      lineHeight: 0.9,
    },
    h1: {
      fontSize: 14,
      lineHeight: 1,
    },
  },
  components: {},
})
